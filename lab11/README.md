# Lab 11: Card Class

Implementation of a playing card class using Python dataclasses.

This lab introduces object-oriented programming concepts by creating a `Card` class with suit and rank attributes. The class includes validation logic, string representation, and basic testing to ensure cards are created correctly.
